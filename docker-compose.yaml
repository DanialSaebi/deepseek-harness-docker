services:
  deepseek-harness:
    build: .

    container_name: deepseek-harness

    ports:
      - "${PORT:-3080}:3080"

    volumes:
      - ./data:/data

    restart: unless-stopped

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"